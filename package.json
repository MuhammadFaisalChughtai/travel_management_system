{
  "name": "travel-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-grade travel booking platform",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
